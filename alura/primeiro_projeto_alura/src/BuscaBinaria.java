import java.util.Random;import java.util.Scanner;public class BuscaBinaria {    public static void main(String[] args) {        // variavel        int tentativas = 1, random = new Random().nextInt(100);        Scanner leitura = new Scanner(System.in);        // loop        while (tentativas <= 5){            System.out.printf("\nDigite um número entre 1 e 100, tentativa %d de 5: ", tentativas);            int chute = leitura.nextInt();            if (chute == random){                System.out.printf("Parabéns, você acertou!");                break;            } else if (chute < random){                System.out.printf("O número digitado é menor que o número gerado");                tentativas++;            } else {                System.out.printf("O número digitado é maior que o número gerado");                tentativas++;            }            if (tentativas == 5){                System.out.printf("Você não conseguiu adivinhar em 5 tentativas, o número era: %d", random);                break;            }        }    }}